spring:
  config:
    activate:
      on-profile: local

  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin

  data:
    mongodb:
     host: localhost
     port: 27017
     database: workloaddb
     auto-index-creation: true

logging:
  level:
    root: INFO
    com.epam.gym.trainerworkloadservice: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.jms: DEBUG